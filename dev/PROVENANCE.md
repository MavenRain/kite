# kite provenance

Every carried file, with its source and what the adaptation changed.
ADAPTED means the file starts from a named source file and keeps its
shape.  NEW means the file has no counterpart in the source
repositories.  KANON is /Users/oobi/Documents/kanon, read only.  BRISK is
/Users/oobi/Documents/brisk, read only.  PIN is
/Users/oobi/Documents/affine-lang-tot-pin at 6d0d48d, read only.  No
agent writes in a read-only tree (M0-PLAN.md:151).

| File | Source and lines | Kind | What changed |
| --- | --- | --- | --- |
| `LICENSE-MIT` | `KANON/LICENSE-MIT`, 21 lines | ADAPTED | Copied with `cp`.  The copyright line `Copyright (c) 2026 Onyeka Obi` is unchanged, because kite carries the same author. |
| `LICENSE-APACHE` | `KANON/LICENSE-APACHE`, 201 lines | ADAPTED | Copied with `cp`.  One line changed:  the appendix placeholder `Copyright [yyyy] [name of copyright owner]` at line 189 becomes `Copyright 2026 Onyeka Obi`, so both licence files name the author (D-A-21). |
| `.gitignore` | `KANON/.gitignore`, 3 lines | ADAPTED | The three rows `_build/`, `*.install` and `.gatework/` are unchanged, because kite ignores the same build tree, the same install stamp and the same gate work directory. |
| `dev/pin-dune.sh` | `BRISK/dev/pin-dune.sh`, 44 lines | ADAPTED | The chpwd guard of `brisk/dev/pin-dune.sh:20-25`, the `-C DIR` option, the self-located work directory and the free command are unchanged.  One line changed:  the exported PATH is `/Users/oobi/.opam/ctxcat-ocaml/bin`, which is the switch of M0-PLAN.md:51 with dune 3.24.0 and OCaml 5.3.0.  The header names kite and its example commands. |
| `dev/house.sh` | `BRISK/dev/house.sh`, 131 lines | ADAPTED | The leg shape, the `report_empty` helper, the `dirs_of` and `search` helpers and the em-dash exclusion globs `'!vendor'`, `'!**/vendor/**'`, `'!_build'`, `'!**/_build/**'` and `'!.git'` are unchanged.  The directory lists become `lib surface test`, and a directory that does not exist is dropped from the ripgrep argument list.  The brisk disclosed mutable window over `vm/exec.ml` is dropped, because kite discloses no window.  The loop leg reads an OCaml loop HEADER, `for NAME =`, and not a bare word, because the kite keyword list holds the iteration word as a language keyword (D-A-23).  Three legs are new:  `no-option-match`, `no-bare-division` and the raw index members of the `no-wildcard-no-partial` leg (D-A-25, narrowed at the fix round to the forms brief 3.12 names).  Seven legs run, against the five of brisk. |
| `dev/bench.sh` | `BRISK/dev/bench.sh`, 71 lines | ADAPTED | The interface `bench.sh NAME CMD`, the timer and the output line are unchanged:  one untimed warm-up, then RUNS timed runs of `/bin/zsh -f -c CMD` under `python3 -P` with `perf_counter_ns` around `subprocess.run`.  Only the header names kite and its provenance row. |
| `dev/denominators.sh` | `BRISK/dev/denominators.sh`, 131 lines | ADAPTED | The five steps, the frozen-record check before any copy, the scratch copy, the `ocamldep -sort` order, the warm-up, the `bench.sh` call and the two output lines `DENOM` and `NUMSHA` are unchanged.  The paths become the kite paths, the `brisk_corpus` key becomes `kite_corpus`, the spine is `examples/m0-spine.kite`, the copy is `$TMPDIR/kite-denom-$$` and the bench sidecar variable is `KITE_DENOM_BENCH`.  Three checks are new:  the spine line count, the spine digest and the one-file rule of the placeholder pin, so a moved spine is a `DENOM-ERROR` and not a silent pass. |
| `dev/trusted-lines.sh` | `BRISK/dev/trusted-lines.sh`, 100 lines | ADAPTED | The self-location, the `wc` reading one file at a time, the `sum_lines` helper, the `--require` flag and the output line shape are unchanged.  The brisk core list and vm list become one kite elaborator list, the eight believed files of M0-PLAN.md section 8, `lib/types.ml`, `lib/row.ml`, `lib/unify.ml`, `lib/infer.ml`, `lib/usage.ml`, `lib/iface.ml`, `lib/ir.ml` and `lib/lower.ml`, against the single bound 2400 of D-M0-5, so the line prints one count:  `TRUSTED-LINES elaborator=0/2400 OK`.  The Stage A battery does not call the script, because the TRUSTED-LINES leg arrives at Stage B. |
| `dev/gates.sh` | `BRISK/dev/gates.sh`, 393 lines | ADAPTED | The one-PASS-or-FAIL-line-per-leg contract of `brisk/dev/gates.sh:12-17`, the every-leg-runs rule, the self-located root of `brisk/dev/gates.sh:19-21`, the single-leg entry `zsh dev/gates.sh --leg NAME` of `brisk/dev/gates.sh:22-24`, the `leg` battery lines of `brisk/dev/gates.sh:375-379`, the case dispatch of `brisk/dev/gates.sh:320-326`, the MEASURE block and the `GATES-OK` or `GATES-FAIL` tail are unchanged.  The battery becomes four legs:  `leg MED BUILD`, `leg FAST HOUSE`, `leg MED PARSE` and `leg SLOW DENOMINATORS`, with the tiers FAST 10, MED 30, SLOW 120 and SUITE 300 seconds.  The PARSE leg builds first, so one leg alone is honest, then runs `test/parse.exe` over the roundtrip, pos and neg lists.  Every other brisk leg stays absent and is never stubbed:  the FLOOR leg and the TRUSTED-LINES leg arrive at Stage B. |
| `dev/denominators.json` | none | NEW | The kite record of the denominator pin.  The `tot_corpus` keys are re-measured on disk this stage:  19 files, 4,976 lines and sha256 `71878111...a730543`.  `kite_corpus` is the Stage A PLACEHOLDER pin of `examples/m0-spine.kite`, two comment lines and no declaration, and the placeholder never changes after this stage.  `ocaml_version` is 5.3.0 and `dune_version` is 3.24.0, which are the versions of the pinned switch. |
| `dev/DENOMINATORS.sha256` | none | NEW | One row, `shasum -a 256 denominators.json` written from inside `dev/`.  The check is `zsh -c "cd dev && shasum -a 256 -c DENOMINATORS.sha256"`. |
| `examples/m0-spine.kite` | none | NEW | The placeholder numerator spine of Stage A.  It holds two comment lines and no declaration, so it is the empty program of D-A-12.  Stage B replaces it with the 1 kloc spine and re-freezes the sidecar. |
| `dev/CARRY.md` | none | NEW | The M1 carries that are not M0 carries. |
| `dev/PROVENANCE.md` | none | NEW | This file. |
| `dev/run-stage-A.sh` | none | NEW | The Stage A runner.  A subagent shell resets its working directory between calls, so one script under the repository holds the gate commands and every path in it is absolute (M0-PLAN.md:150). |
| `lib/*.ml`, `surface/*.ml` | none | NEW | The kite front end is a rewrite and not a port.  No line of code comes from a sibling tree. |
| `dev/run-stage-B.sh` | `dev/run-stage-A.sh`, 309 lines | NEW in round B1, appended by every later round | The Stage B runner.  A subagent shell resets its working directory between calls, so one script under the repository holds every gate command and every mutation command of the stage and every path in it is absolute (M0-PLAN.md:150).  The `set -u` header, the REPO and SCRATCH names, the one function per gate, the `print -r --` evidence lines and the case dispatch at the tail come from the Stage A runner.  Round B4 appends SB-G13 to SB-G20 and the six mutation commands SB-M1 to SB-M6. |
| `dev/floor-corpus.txt` | none | NEW in round B4 | The floor corpus of D-M0-4, one bare file name per line in `ocamldep -sort` order:  the shortest prefix of `PIN/lib/*.ml` in that order whose total `wc -l` is at or above 500, at or under 2,000 and within a factor of two of the spine kloc.  The six names total 572 lines.  The FLOOR leg copies them to `$TMPDIR/kite-floor-$$` per run and removes the copy after.  The file names the corpus;  the pin content is frozen at 6d0d48d and gated by SB-G17. |
| `dev/floor-corpus.sha256` | none | NEW in round B4 | One row, `shasum -a 256 floor-corpus.txt` written from inside `dev/`, so `shasum -a 256 -c` reads it there (D-B-31).  The digest of the corpus CONTENT is re-measured by the FLOOR leg on every run and printed as `flo_sha`. |
| `dev/floor-corpus.lines` | none | NEW in round B4 | One line, the total `wc -l` of the named pin files, which is the number the size rule of the FLOOR protocol reads (D-B-31).  The FLOOR leg re-measures it and a stale value fails the leg. |
| `examples/m0-spine.sha256` | none | NEW in round B4 | One line, the output of `shasum -a 256` over the spine taken from inside `examples/`. |
| `examples/m0-spine.lines` | none | NEW in round B4 | One line, the output of `wc -l < examples/m0-spine.kite` with the surrounding space removed. |
| `dev/gates.sh` | this file, row 21 | CHANGED in round B4 | Three legs join the four of Stage A:  CHECK (D-B-26), TRUSTED-LINES (D-B-28) and FLOOR, which is GATE M0 (D-B-24).  The PARSE leg list grows with every twin of `test/neg` and the spine, so it prints `fixtures=46` (D-B-27).  Line 1 reads `stage=B`.  The battery is seven legs and every leg still runs when an earlier one failed. |
| `dev/denominators.json` | this file, row 22 | CHANGED in round B4 | The ONE edit of D-B-29:  the `kite_corpus` record is re-frozen on the 1 kloc spine, `lines` 2 becomes 1055 and `sha256` becomes `036cf354...759ff3f9`.  Its `files` list stays the one entry `examples/m0-spine.kite`.  The `tot_corpus` record is byte for byte unchanged, so the pin, its 19 file names, its 4,976 lines and its digest stand.  `dev/DENOMINATORS.sha256` is rewritten in the same edit. |
| `examples/m0-spine.kite` | this file, row 24 | CHANGED in round B4 | The 2-line Stage A placeholder becomes the frozen 1 kloc numerator corpus of D-M0-3:  1,055 lines, one program of many declarations that all check clean, every M0 form of `surface/ast.ml`, and 30 `-1>` binders for the use count pass. |
| `.gitignore` | this file, row 15 | CHANGED in round B4 | ONE added row, `*.kir`.  The FLOOR leg emits `examples/m0-spine.kir` beside the source five times per gate run, and SB-G18 fails on a tracked `.kir` path and on an untracked one alike, so the build artifact of D-B-17 is ignored.  The three Stage A rows `_build/`, `*.install` and `.gatework/` are unchanged. |
| `test/regress.ml` | none | NEW in Stage B review | Semantic probes for row cycles, duplicate fields, value restriction, lexical affine use and interface generality.  Positive neighbors guard against an overbroad refusal.  `test/dune` builds the executable. |
| `dev/gates.sh` | this file | CHANGED in Stage B review | CHECK requires at least thirteen positives and six twins, then runs `test/regress.exe`.  A failed regression fails the leg.  The parser corpus and the timing limits stay fixed. |
| `dev/run-stage-B.sh` | this file | CHANGED in Stage B review | The runner locates its own tree, owns a unique scratch directory and propagates failed assertions.  A mutant must apply its edit, build successfully and fail the intended semantic check before it counts as killed. |
| `README.md` | this file | CHANGED in Stage B review | Documents the implemented driver, separate compilation and the seven Stage B gate legs. |
| `runtime/*.ml`, `runtime/*.mli`, `test/cluster.ml`, `test/kubelet.ml`, `test/runtime_suite.ml` | none | NEW in M1-A | Original native control models and behavioral tests.  Design constraints follow the M1 plan;  no sibling implementation is copied. |
| `dev/runtime-mutations.py` | none | NEW in M1-A | Builds each deliberate safety regression in a disposable copy and requires its named test to fail. |
| `dev/gates.sh`, `dev/house.sh` | these files | CHANGED in M1-A | Add the native RUNTIME leg and include runtime sources and interfaces in HOUSE.  Preserve all M0 gate legs and limits. |
| `browser/glue.js` | `/Users/oobi/Documents/ocaml-tea/lib/tea_client_run/idb.ml:127-149`, `tab_lock.ml:32-81` | ADAPTED in M1-B | Keep commit-level completion and one-shot acquisition results.  Replace Ojs and Tea types with JS outcomes.  Add releasable leases, atomic epoch and sequence fences, and an append-only log.  Sibling files remain read only. |
| `browser/model.ml`, `browser/dune` | none | NEW in M1-B | Export the original native models through the installed js_of_ocaml 6.2.0.  Validate wire fields and preserve opaque lifecycle state. |
| `browser/control.js`, `browser/node-host.js`, `browser/pod.js`, `browser/page.js`, `browser/index.html` | none | NEW in M1-B | Execute model effects with ticket and generation checks.  Demonstrate placement and a fresh Wasm increment fixture.  No implementation is copied from tab-cluster-spike. |
| `test/glue.test.mjs`, `test/node-host.test.mjs`, `test/control.test.mjs`, `test/browser-audit.test.py`, `test/browser-probe.js`, `test/browser.html`, `dev/browser-test.mjs` | none | NEW in M1-B | Test boundary refusals, lifecycle races with the compiled model, and real Chrome behavior through CDP.  Record probe limits separately from results. |
| `dev/browser-audit.py`, `dev/browser-pipeline.sh` | none | NEW in M1-B | Check the browser effect boundary and 300 physical-line GLUE limit.  Include fresh js_of_ocaml emission and browser asset assembly in each FLOOR numerator sample. |
| `dev/gates.sh`, `dev/house.sh`, `dev/pin-dune.sh` | these files | CHANGED in M1-B | Add BROWSER, include browser OCaml in HOUSE with two exact FFI conversion allowances, retain the FLOOR protocol, and pin opam library paths as well as executable paths. |
| `runtime/eval.ml`, `runtime/artifact.ml`, `browser/program.ml`, `browser/source.js` | none | NEW in M1-C | Pure checked-IR execution, offline data artifact emission, browser decoding and explicit host continuation driver. The checker is absent from the shipped evaluator bundle. No sibling language implementation is copied. |
| `browser/pod.js` | `/Users/oobi/Documents/tab-cluster-spike/web/pod.wat:1-16` | ADAPTED in M1-C | Embed the 72-byte result of `wat2wasm` for the required Stage 0 `step` function. Execute `step(200000)` per tick, with checksum `-1734620768`. Keep Kite's nested-worker ownership, lock and publication protocol. |
| `dev/drive.mjs`, `test/acceptance.html`, `test/acceptance-bridge.js` | `dev/browser-test.mjs` in this repository | ADAPTED in M1-C | Reuse the CDP pipe, ephemeral server, isolated profile and cleanup pattern. Add source-directed operations, FIFO queue observations, duplicate refusal/exit evidence and full hidden-tab timing checks. |
| `test/acceptance.kite`, `dev/m1-source-runner.mjs`, `test/eval_test.ml`, `test/program.test.mjs`, `test/source.test.mjs`, `test/acceptance-bridge-test.mjs` | none | NEW in M1-C | Source-controlled scenarios, a narrow host adapter, native/browser semantic checks and asynchronous boundary regressions. |
| `browser/control.js`, `browser/node-host.js`, `test/control.test.mjs`, `test/node-host.test.mjs` | these files | CHANGED in M1-C | Fix epoch-claim/reconciliation and resume/freeze races, cover startup watchdogs, retry termination while retaining slots, and record bounded duplicate-refusal evidence. |
| `bin/kite.ml`, dune stanzas, `.gitignore`, gate scripts and browser audit | these files | CHANGED in M1-C | Add executable artifacts and native run, isolate evaluator from compiler dependencies, require SOURCE and full ACCEPTANCE, include both js_of_ocaml bridges in every FLOOR sample, and retain all existing corpus pins and bounds. |
| `runtime/feed.ml`, `runtime/service.ml`, `runtime/volume.ml`, `runtime/taint.ml`, `runtime/manifest.ml` and their interfaces | none | NEW in M2-A | Original pure committed-prefix, named-channel, volume transaction, visibility and admission models. Follow design amendments A3, A8 and A10; no sibling implementation copied. Browser effects and source binding remain M2-B. |
| `test/feed_test.ml`, `test/service_test.ml`, `test/volume_test.ml`, `test/taint_test.ml`, `test/manifest_test.ml` | none | NEW in M2-A | Native recovery, replay, writer-fence, lifecycle, policy-composition and admission cases, using the existing Runtime_suite harness. |
| `runtime/cluster.ml`, `runtime/cluster.mli`, runtime and test dune stanzas, `dev/gates.sh` | these files | CHANGED in M2-A | Add intersecting new-start restrictions and the DURABLE native gate with exact suite counts. Keep M1's default policy and every inherited gate and bound. |
| `dev/durable-mutations.py` | `dev/runtime-mutations.py` in this repository | ADAPTED in M2-A | Retain disposable source copies and buildable named semantic mutations; add exact one-case baseline selection checks, native-only builds, bounded diagnostics and cleanup on failure. |
| `dev/M2-PLAN.md`, `dev/stage-M2-A-brief.md`, `dev/M2-BUILD-LOG.md`, `dev/m2-a-validation/` | none | NEW in M2-A | Separate native, integration and browser acceptance stages; pin twelve future browser faults and record actual native and inherited gate evidence. |
